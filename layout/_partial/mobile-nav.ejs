<nav id="mobile-nav">
    <a class="no-style header-link" href="
    <% if (is_home()) { %>
      <%= url_for(config.root) %>
    <% } else { %>
      <%= url_for(page.lang) %>
    <% } %>
    "><%= __('index') %></a>
	<a href="<%= url_for(page.lang + '/archives') %>"><%= __('archives') %></a>
	<a target="_blank" href="<%= page.github || (config.github + '/edit/master/source/' + (page.source || '../' + __('cate.path'))) %>"><%= __('edit') %></a>
	<a href="<%= url_for('/readme') %>">README</a>
	<a target="_blank" href="<%= config.github %>"><%= __('github') %></a>
</nav>